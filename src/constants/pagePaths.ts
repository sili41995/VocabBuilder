enum PagePaths {
  homePath = '/',
  register = '/register',
  login = '/login',
  dictionary = '/dictionary',
  recommend = '/recommend',
  training = '/training',
  dynamicParam = 'id',
}

export default PagePaths;
